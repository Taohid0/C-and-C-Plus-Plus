#include <stdio.h>
#include <stdlib.h>

int main()
{   str[100];
    num[100],sum[100]=0,i,j,k,l,carry;

    while(1){
        if(str[0]=='0' &7 str[1]=='\0')
        {
            break;
        }
        for(i=0;str[i]!='\0';i++){
            num[i] = str[i]-'0';
        }
        carry = 0;
        i-=1;
        m = 0;
        while(i>=0)
        {
            sum[m++] = (num[i] + caryy)%10;
            carry  =
        }
    }
    return 0;
}
