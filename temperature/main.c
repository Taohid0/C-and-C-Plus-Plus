#include <stdio.h>
#include <stdlib.h>

int main()
{int celsius,Fahrenheit;
    printf("what is the temperature is Celsius unite?\n");
    scanf("%d",&celsius);
    Fahrenheit=1.8*celsius+32;
    printf("the temperature in Fahrenheit unite is %d",Fahrenheit);
    return 0;
}
