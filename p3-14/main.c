#include <stdio.h>
#include <conio.h>

int main()
{int num,sum=0;


        while(num>=0)    {
    printf("enter numbers\n");
    scanf("%d",&num);
    sum+=num;
    }
    printf("%d",sum);

    getch();

    return 0;
}
