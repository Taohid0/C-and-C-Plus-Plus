#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);

    string s;

    while(1)
    {
        if(s==".")
            break;

        int len = (int)s.length();

        for(int i = 0;i<)
    }
    return 0;
}
