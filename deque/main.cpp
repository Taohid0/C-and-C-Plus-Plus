#include <bits/stdc++.h>


using namespace std;

int main()
{
   vector<int>v;
   int n,num;
   cin>>n;
   for(int i = 0;i<n;i++){
    cin>>num;
    v.push_back(num);
   }
   sort(v.begin(),v.end());
    sum = v[n-1];
    for(int i = n-2;i>=0;i--){
        if(v[i])
    }

    return 0;
}
