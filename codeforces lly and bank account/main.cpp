#include <bits/stdc++.h>

using namespace std;

int main()
{
    string s;
    cin>>s;
    if(s[0]!=='-')
    {
        cout<<s<<endl;
    }

    else {
        int indx;
        int len=(int)
    }

    return 0;
}
