#include <stdio.h>
#include <stdlib.h>

int main()
{int count;

    //printf("how many numbers do u want to caltulate?\n");
   // scanf("%d",&number);

    for(count = 0;count<=100;count+=5)
       // sum+=count;
    printf("%d\n",count);
    return 0;
}
