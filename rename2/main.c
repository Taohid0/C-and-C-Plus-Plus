#include <stdio.h>
#include <stdlib.h>

int main()
{ char filename[10],renm[10];

    printf("which file do you want to rename?");
    gets(filename);
    printf("what's the new name?");
    gets(renm);

    rename("d:/data/%s.txt" "d:/data/%s.txt",);

    return 0;
}
