#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
   {int num,num2,i;

    printf("num\n");
    scanf("%d",&num);
    num2=num-1;
    i=pow(10,num2);
    printf("%d",i);
    return 0;
}
